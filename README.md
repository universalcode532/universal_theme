# universal_theme

Reusable Flutter theme package with GetX support.

## Installation

Add to your app’s `pubspec.yaml`:

```yaml
dependencies:
  universal_theme:
    git:
      url: https://github.com/universalcode532/universal_theme.git
      ref: main
